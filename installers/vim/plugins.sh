#!/bin/bash
#DIR="$( cd -P "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#CP="/bin/cp -vfr"

printf "\nInstalling vim plugins\n"

rm -rfv ~/.vim/plugin
#mkdir -p ~/.vim/plugin
#PLUGINS_DIR="`eval echo ${DIR//>}/../../addons/vim/plugin`"
#$CP $PLUGINS_DIR ~/.vim

